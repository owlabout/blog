@import url("https://fonts.googleapis.com/css?family=Roboto:100,400|VT323");
@require '../../../node_modules/@vuepress/theme-default/styles/code'
@require '../../../node_modules/@vuepress/theme-default/styles/custom-blocks'
@require '../../../node_modules/@vuepress/theme-default/styles/arrow'
@require '../../../node_modules/@vuepress/theme-default/styles/toc'
@require 'icons'

* {
  box-sizing: border-box;
}

html
  font-size: 62.5%
  margin: 0

body
  margin 0
  padding 0
  font-size: 1.6rem;
  background-color: $background;
  color: $textColor
  font-family "Roboto", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif

heading(size) {
  margin: -8rem 0 0 0;
  padding: 9rem 0 0;
  font-family: "VT323", monospace;
  font-size: size;
  font-weight: normal;
  position: relative;

  .header-anchor {
    position: absolute;
    left: -1.5ex;
    opacity: 0;
  }

  &:hover .header-anchor {
    opacity: 1;
    &:hover {
      color: $primary;
    }
  }
}


h1 {
  heading(4.8rem)
  }
h2 {
  heading(3rem)
}
h3 {
  heading(2.4rem)
}

a {
  font-weight 500
  color $accentColor
  text-decoration none
}

p a code
  font-weight 400
  color $accentColor

kbd
  background #eee
  border solid 0.15rem #ddd
  border-bottom solid 0.25rem #ddd
  border-radius 0.15rem
  padding 0 0.15em

blockquote
  font-size 1.6rem
  color #999
  border-left .5rem solid #dfe2e5
  margin 0.5rem 0
  padding .25rem 0 .25rem 1rem
  & > p
    margin 0

ul, ol
  padding-left 1.2em

strong
  font-weight 600

code, kbd, .line-number
  font-family source-code-pro, Menlo, Monaco, Consolas, "Courier New", monospace

p, ul, ol
  line-height 1.7

hr
  border 0
  border-top 1px solid $borderColor

table
  border-collapse collapse
  margin 1.6rem 0
  overflow-x: auto
  margin: 0 auto;

tr
  border-top 1px solid #dfe2e5
  &:nth-child(2n)
    background-color #f6f8fa

th, td
  border 1px solid #dfe2e5
  padding 1em 1.6em

.content
  &:not(.custom)
    max-width: 1400px;
  pre, pre[class*="language-"]
    code
      color $textColor

div[class*="language-"]
  border-radius 0
  border-left: 4px solid $yellow
